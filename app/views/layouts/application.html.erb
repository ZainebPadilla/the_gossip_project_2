<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "The Gossip Project" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
 
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

  

<nav class="navbar bg-dark border-bottom border-body" data-bs-theme="dark">
  <ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
    <li class="nav-item">
      <a class="nav-link active" id="pills-home-tab" href="<%= root_path %>" role="tab" aria-controls="pills-home" aria-selected="true">Home</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="pills-profile-tab" href="<%= team_path %>" role="tab" aria-controls="pills-profile" aria-selected="false">Team</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" id="pills-contact-tab" href="<%= contact_path %>" role="tab" aria-controls="pills-contact" aria-selected="false">Contact</a>
    </li>
  </ul>
  
    <div class="d-flex align-items-center"> 
      
        <% if !current_user %>
  <%= link_to "Connexion", login_path, class: "btn btn-outline-light ms-2" %> |
  <%= link_to "Inscription", new_user_path, class: "btn btn-outline-light ms-2" %>
<% else %>
  <%= current_user.email %>
  <p class="text-white m-0">Bienvenue, <%= current_user.email %> !</p>
  <%= link_to "Déconnexion", logout_path, method: :delete, class: "btn btn-outline-light ms-2" %>
  <%= link_to "Créer un Nouveau gossip", new_gossip_path %>
<% end %>
    </div>
   
</nav>
    <%= yield %>
  </body>
</html>
